class GlobalProperties {
  static init(app) {

    // console.log(app);
    // app.provide('BaseStore', useBaseStore());
    // app.config.GlobalProperties.a = "a"
  }
}

export default GlobalProperties;
